# Thashmi Fernando Portfolio

## Run locally

```bash
npm install
npm run dev
```

Open the URL displayed in the terminal.

## Build for production

```bash
npm run build
```

The production files will be generated inside the `dist` folder.
